{
  "configuration": {
    "dist": "atomic.{type}.{ext}"
  },
  "bundle": {
    "src/atomic.js": {
      "description": "Small initialisation script and animation for the canvas"
    },
    "src/defaults.styl": {
      "description": "Changable variables to easily customize Atomic"
    },
    "src/atomic.styl": {
      "description": "Styling for the progress bar",
      "dependencies": [ "src/defaults.styl" ]
    }
  }
}
